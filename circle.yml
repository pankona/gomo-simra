general:
    build_dir: example

dependencies:
    pre:
        - go get golang.org/x/mobile/cmd/gomobile
        - gomobile init -v
    override:
        - echo "dependencies here!"
        - pwd
        - gomobile build -v

test:
    override:
        - echo "test here!"
