dependencies:
    pre:
        - go get golang.org/x/mobile/cmd/gomobile
        - gomobile init -v
    override:
        - echo "dependencies here!"

general:
    build_dir: example

test:
    override:
        - echo "test here!"
